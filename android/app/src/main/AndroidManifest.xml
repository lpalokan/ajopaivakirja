<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <application
        android:label="Ajopäiväkirja"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Explicit FileProvider so open_filex's ACTION_VIEW intent works
             even though share_plus and image_picker also bundle providers.
             Without this the manifest merger can drop open_filex's provider
             and "Avaa sovelluksessa" silently fails. -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileProvider"
            android:exported="false"
            android:grantUriPermissions="true"
            tools:replace="android:authorities">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/filepaths"
                tools:replace="android:resource" />
        </provider>


        <!-- Reminds the driver to log a trip when the car's Bluetooth
             connects or disconnects. Declared here rather than registered at
             runtime on purpose: ACL_CONNECTED / ACL_DISCONNECTED are exempt
             from the API 26+ implicit-broadcast restrictions, so the system
             starts the process to deliver them even when the app is not
             running. exported=true because the sender is the system. -->
        <receiver
            android:name=".CarBluetoothReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.bluetooth.device.action.ACL_CONNECTED"/>
                <action android:name="android.bluetooth.device.action.ACL_DISCONNECTED"/>
            </intent-filter>
        </receiver>

        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!-- Lets the "Open in an app" feature resolve viewer apps
             (Sheets/Excel/PDF viewers) on Android 11+ (open_filex). -->
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:mimeType="*/*"/>
        </intent>
    </queries>
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <!-- Reading the paired-device list and matching the car in the ACL
         broadcast. Runtime ("Nearby devices") from Android 12; install-time
         BLUETOOTH before that. -->
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT"/>
    <uses-permission android:name="android.permission.BLUETOOTH"
        android:maxSdkVersion="30"/>
    <uses-permission android:name="android.permission.CAMERA"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION"/>
    <!-- Declared but NOT currently acquired: LocationService.
         tripLocationSettings sets ForegroundNotificationConfig.
         enableWakeLock = false, so geolocator's location foreground service
         takes neither the PARTIAL_WAKE_LOCK nor the WifiLock it would
         otherwise hold for the whole trip (#91 — they kept the phone out of
         doze all day and were the single largest item in the battery
         report).

         Kept in the manifest because the pairing is a trap this project has
         already fallen into once. When the flag WAS set, the permission
         arrived transitively through flutter_background_service; dropping
         that unused dependency (#87) took it out of the merged manifest and
         GeolocatorLocationService.obtainWakeLocks() then threw
         SecurityException inside the geolocator EventChannel's onListen.
         Flutter clears the active event sink when onListen throws, so the
         trip position stream went permanently silent while the foreground
         service and its notification carried on looking healthy, and
         "Oletko perillä?" lost the GPS evidence that keeps it from firing
         mid-drive. location_service_test.dart ties the two together so
         re-enabling the flag cannot silently repeat that. -->
    <uses-permission android:name="android.permission.WAKE_LOCK"/>
    <!-- Activity recognition: used to suppress the 5-min "Oletko perillä?"
         reminder while the device reports the user is still in_vehicle. -->
    <uses-permission android:name="android.permission.ACTIVITY_RECOGNITION"/>
    <uses-permission android:name="com.google.android.gms.permission.ACTIVITY_RECOGNITION"/>
    <!-- "Tarkista päivitykset": hand the downloaded APK to the system
         package installer. The user must still enable "Install unknown
         apps" for this app the first time (Android 8+ requirement). -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES"/>
</manifest>
